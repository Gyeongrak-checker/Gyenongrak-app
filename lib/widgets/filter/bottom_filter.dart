import 'package:flutter/material.dart';

class BottomFilter extends StatelessWidget {
  const BottomFilter({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(height: 65.0, color: Colors.green);
  }
}
